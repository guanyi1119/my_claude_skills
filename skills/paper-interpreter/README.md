# 论文解读 Skill (paper-interpreter)

一个用于深度解读学术论文的skill，支持中英文论文和PDF/文本格式，包含论文归档管理功能。

## 功能特性

- 📄 **多格式支持**：支持PDF文件和纯文本输入
- 🌐 **中英文论文**：自动识别并处理中英文论文
- 📊 **结构化分析**：提取贡献点、方法论、实验结果等关键信息
- 🔬 **消融实验分析**：专门分析论文中的消融实验
- 💡 **术语解释**：自动解释专业术语
- 🎯 **交互式输出**：可展开的详细分析结构
- 📚 **论文归档管理**：
  - 可选择归档或不归档论文
  - 按标签分类保存
  - Excel文件管理，支持超链接直接打开
  - 自动重命名和文件管理

## 文件结构

```
paper-interpreter/
├── SKILL.md                           # 主skill定义文件
├── README.md                           # 说明文档
├── scripts/
│   ├── pdf_extractor.py              # PDF文本提取工具
│   └── archiver.py                   # 论文归档管理脚本
├── references/
│   └── paper_analysis_template.md    # 论文分析模板
├── evals/
│   └── evals.json                    # 测试用例
└── interpretations/                  # 临时解析结果目录（运行时创建）
```

## 归档位置（运行时创建）

```
{project_workspace}/
└── paper_archive/                    # 归档目录（位于当前工作目录）
    ├── paper_archive.xlsx           # 归档Excel文件
    ├── pdf/                          # 论文PDF文件
    └── md/                           # 论文解读报告
```

## 安装依赖

如需使用PDF解析和归档功能，请安装以下库：

```bash
pip install pdfplumber openpyxl
```

## 使用方法

### 基本使用

当用户要求解读论文时，skill会自动触发。你可以：

1. **直接粘贴论文文本**：
   ```
   请解读这篇论文：[粘贴论文内容]
   ```

2. **提供PDF文件路径**：
   ```
   请解读这个PDF论文：path/to/paper.pdf
   ```

3. **询问特定问题**：
   ```
   这篇论文的主要贡献点是什么？
   ```

### 输出结构

分析结果会以交互式格式呈现：

- 🎯 **快速摘要**：一句话总结
- 📋 **详细分析**（可展开）：
  - 研究背景与问题
  - 主要贡献点
  - 方法论细节
  - 实验设置
  - 消融实验分析
  - 主要结果
  - 结论与未来工作
  - 关键术语解释
- 📚 **归档询问**：询问是否需要归档论文

### 归档流程

在解析完成后，skill会询问是否需要归档论文：

#### 如果不归档
根据文件来源进行不同处理：

- **临时文件（在线链接下载）**：
  - 删除临时生成的PDF和MD文件
  - 删除临时提取的文本文件
  - 结束流程

- **本地文件（用户提供的本地PDF）**：
  - 询问用户如何处理文件，提供选项：
    1. 删除所有临时文件，但保留原始PDF
    2. 删除所有文件（包括原始PDF）
    3. 保留所有文件（不删除任何文件）

#### 如果归档
1. **输入归档原因**：描述为什么要归档这篇论文
2. **输入归档标签**：用于分类（如"计算机视觉"、"自然语言处理"等）
3. **自动处理**：
   - 创建/更新Excel归档文件（保存到当前工作目录的paper_archive文件夹）
   - 重命名并保存文件到对应目录
   - 添加超链接到Excel文件
4. **原始文件处理**：
   - 对于临时下载的PDF文件：归档后自动删除原始文件
   - 对于本地PDF文件：询问用户是否删除原始文件

### 归档文件位置

- **Excel文件**：`{project_workspace}/paper_archive/paper_archive.xlsx`（位于当前工作目录下）
- **PDF文件**：`{project_workspace}/paper_archive/pdf/`（重命名后的）
- **解读报告**：`{project_workspace}/paper_archive/md/`（重命名后的）

### Excel文件结构

每个标签对应一个Sheet页，包含以下列：
- **标题**：论文标题
- **年份**：发表年份
- **作者单位**：作者所属机构
- **收录会议/杂志**：CVPR、ICML、IEEE等
- **关键词**：关键词（逗号分隔）
- **归档原因**：用户输入的归档原因
- **归档日期**：归档时间戳
- **PDF路径**：本地PDF文件路径（超链接）
- **解读报告路径**：本地MD文件路径（超链接）

## 主要功能说明

### 1. 论文概览

快速提取论文的基本信息，包括标题、作者、发表信息等。

### 2. 贡献点提取

重点突出论文的3-5个主要贡献，每个贡献包含：
- 具体描述
- 创新点
- 重要性

### 3. 方法论分析

详细分析技术方法：
- 核心算法/模型架构
- 关键技术步骤
- 数学公式
- 实现细节

### 4. 消融实验分析

专门分析论文中的消融实验：
- 实验目的
- 实验设计
- 实验结果
- 结论

### 5. 术语解释

解释论文中的3-5个重要专业术语。

## 示例

### 示例1：解读Transformer论文

```
用户：请解读这篇关于Transformer的论文摘要

输出：
# 📑 论文解读：Transformer模型改进

## 🎯 快速摘要
本文通过引入稀疏注意力机制和分层编码策略，改进了Transformer架构，在保持良好性能的同时显著提高了计算效率。

## 📋 详细分析
...
```

### 示例2：分析特定贡献

```
用户：这篇YOLOv7论文的主要贡献点是什么？

输出：
# 📑 论文解读：YOLOv7目标检测方法

## 🌟 主要贡献点

贡献点1：级联卷积网络架构
- 具体描述：结合特征金字塔网络和空间注意力机制
- 创新点：...
```

## 开发说明

### 测试用例

运行测试用例以验证skill功能：

```bash
# 测试用例位置
evals/evals.json
```

### 扩展功能

如需添加新功能，可以：
1. 修改 `SKILL.md` 中的工作流程
2. 在 `scripts/` 中添加新的辅助脚本
3. 更新 `references/` 中的模板文件

## 常见问题

**Q: 支持哪些语言的论文？**

A: 主要支持中英文论文，能自动检测语言并调整分析策略。

**Q: PDF解析需要什么依赖？**

A: 需要安装 `pdfplumber` 库。

**Q: 如何提高分析准确性？**

A: 提供完整的论文内容（特别是引言、方法论、实验章节）可以提高分析质量。

## 许可证

此skill可自由使用和修改。
